# D-MSTCN Mathematical Formulation — Code-Alignment Ledger

Last updated: 2026-07-19 21:15 IST

## Purpose and authority

This document will contain only formulations derived from inspected executable code
and confirmed by tests. The implementation is authoritative; manuscript equations are
inputs to audit, not proof of implementation behavior. Candidate equations below are
explicitly marked pending until the isolated checkout is audited.

## Verification ledger

| Topic | Candidate formulation | Required code evidence | State |
|---|---|---|---|
| Causal convolution | Output at time `t` may depend only on input indices `<= t` | padding/cropping implementation plus perturbation test | pending_audit |
| Residual-block receptive field | For two stride-one convolutions per block, `R = 1 + 2(K-1) sum_l d_l` | exact block graph plus gradient and perturbation support | pending_audit |
| FiLM personalization | One stored subject embedding row of dimension `d_s`; scale/shift vectors are generated by a shared map | embedding and FiLM module parameter shapes | pending_audit |
| CSAG fusion | Must be transcribed from the implemented score, normalization, and fusion operations | forward method tensor operations and shapes | pending_audit |
| Training objective | Must match the configured loss, class weighting, and any auxiliary terms in the trainer | trainer loss construction and resolved run configs | pending_audit |
| TCP staleness | May use `Delta_b = v_global - v_b` only if version counters in code implement that definition | TCP state transitions and invariant tests | pending_audit |
| Statistical estimands | Participant/grouped-fold effects are primary; seed variation is secondary | evaluation aggregation and bootstrap code | pending_audit |

## Prohibited shortcuts

- Do not copy the rejected manuscript's receptive-field, parameter, convergence, or
  synchronization claims into this document unless executable behavior confirms them.
- Do not convert generated activations into persistent parameter counts.
- Do not call a single-server simulation a multi-node experiment.
- Do not express a significance claim whose sampling unit or attainable p-value does
  not support it.

The next update will replace candidate text with exact symbols, tensor shapes, losses,
and invariants traced to file/line locations and executable tests.
