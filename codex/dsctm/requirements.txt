# Top-level dependencies. PyTorch built for CUDA 12.4 (RTX 4060 Ti, sm_89).
# Install torch first from the CUDA index, then the rest:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
#   pip install -r requirements.txt
# Exact pinned versions used for these results: see requirements-lock.txt
torch>=2.6
numpy
scipy
pandas
scikit-learn
matplotlib
seaborn
pyyaml
openpyxl
tqdm
einops
thop
tensorboard
rich
pytest
